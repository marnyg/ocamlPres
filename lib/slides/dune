(executable
  (name test)
  (modes js)
  (libraries js_of_ocaml js_of_ocaml-ppx)
  (preprocess (pps js_of_ocaml-ppx)))

(rule
  (targets test.js)
  (deps (:test_exe test.bc.js))
  (action (copy %{test_exe} %{targets})))

